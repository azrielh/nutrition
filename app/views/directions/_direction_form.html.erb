<%= simple_form_for [@recipe, @direction], remote: true, html: {class: "form-horizontal"} do |f| %>
  <%= f.input :instruction, label: false, placeholder: "add directions here...", input_html: { class: 'form-input form-control' } %>
  <%= f.submit "Add Step", class: "btn btn-primary" %>
<% end %>
